# Strumenti di sviluppo (lint, type-check, test). Runtime -> requirements.txt
-r requirements.txt

ruff==0.8.4
mypy==1.14.0
pre-commit==4.0.1

# Type stubs for untyped third-party libs (mypy import-untyped, not covered by
# ignore_missing_imports). Required for a clean `make typecheck` in a fresh env.
types-PyYAML==6.0.12.20240917
types-python-dateutil==2.9.0.20241206
