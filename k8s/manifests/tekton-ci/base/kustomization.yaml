apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: cashato-ci

resources:
  - namespace.yaml
  - serviceaccount.yaml
  - sealedsecret-git-basic-auth.yaml
  - sealedsecret-dockerconfig.yaml
  - sealedsecret-webhook-secret.yaml
  - sealedsecret-gitea-admin.yaml
  - task-lint-test.yaml
  - pipeline.yaml
  # Triggers (C7c-d): build-on-push via a Gitea webhook → in-cluster EventListener.
  - triggers-rbac.yaml
  - triggerbinding.yaml
  - triggertemplate.yaml
  - eventlistener.yaml
  - webhook-job.yaml
