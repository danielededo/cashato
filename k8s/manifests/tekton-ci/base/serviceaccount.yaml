# ServiceAccount the PipelineRuns run under. Auth to Gitea (clone + registry
# push) is supplied via workspaces (basic-auth / dockerconfig SealedSecrets),
# not SA-linked secrets, so this SA needs no attached credentials.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: cashato-ci
