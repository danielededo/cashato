apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# The InferenceService carries its own namespace (cashato-ml). The predictor image
# tag is hardcoded here (kustomize's images transformer doesn't rewrite fields in
# CRDs); swap to a Harbor tag in C7.
resources:
  - inferenceservice.yaml
