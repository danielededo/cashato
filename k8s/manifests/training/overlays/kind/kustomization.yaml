apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

# Central place to swap the image (kind-loaded; out of CI scope, so no SHA pinning).
images:
  - name: cashato/train
    newTag: dev
