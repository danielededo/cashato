apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# All resources land in cashato-ml (the Namespace object itself is unaffected).
namespace: cashato-ml

resources:
  - namespace.yaml
  - sealedsecret-ml-reader.yaml
  - register-champion-job.yaml
  - cronjob.yaml
