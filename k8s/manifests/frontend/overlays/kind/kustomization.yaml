apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

# CI pins this to the SHA registry ref via the cashato-deploy config repo;
# `dev` is the seed default (local kind-load).
images:
  - name: cashato/frontend
    newTag: dev
