# Dedicated namespace for MinIO (object storage backbone), like nats. The bucket
# holds uploaded statements; services reach it at minio.minio.svc:9000.
apiVersion: v1
kind: Namespace
metadata:
  name: minio
