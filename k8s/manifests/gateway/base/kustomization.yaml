apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# No `namespace:` transformer here: GatewayClass is cluster-scoped and the
# Gateway carries its own namespace, so each resource sets its scope explicitly.
resources:
  - namespace.yaml
  - gatewayclass.yaml
  - gateway.yaml
