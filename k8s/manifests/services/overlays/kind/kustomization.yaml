apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

# Central place to swap the shared service image (kind-loaded now; Harbor tag in C7).
images:
  - name: cashato/svc
    newTag: dev
