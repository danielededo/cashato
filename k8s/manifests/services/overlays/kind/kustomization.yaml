apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

# Central place to swap the shared service image; CI pins a Gitea-registry SHA tag over this.
images:
  - name: cashato/svc
    newTag: dev
